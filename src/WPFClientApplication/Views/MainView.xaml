<Window
  x:Class="WPFClientApplication.Views.MainView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:local="clr-namespace:WPFClientApplication.Views"
  xmlns:viewModels="clr-namespace:WPFClientApplication.ViewModels"
  mc:Ignorable="d"
  d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False, Type={x:Type viewModels:MainViewModel}}"
  Title="MainView"
  Height="450"
  Width="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Button
      Grid.Row="0"
      Grid.Column="0"
      Width="100"
      Height="25"
      Margin="10"
      Content="Login"
      Command="{Binding LoginCommand}" />
  </Grid>
</Window>